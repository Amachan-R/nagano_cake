<main>
  <h1>ジャンル編集</h1>

  <%= form_with model:@genre, url:"/admin/genres/#{@genre.id}", local:true do |f| %>
    <p>ジャンル名</p>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存' %>
  <% end %>

</main>